// D1Database/Cloudflare.Env come from the generated worker-configuration.d.ts
// (run `npx wrangler types`, see README). This file adds only the
// `cloudflare:test` module types (env, etc.) that test files import, which
// aren't part of that generated file.
/// <reference types="@cloudflare/vitest-pool-workers/types" />
